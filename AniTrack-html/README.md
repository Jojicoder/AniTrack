# AniTrack School HTML Version

This version is for HTML/CSS/JavaScript presentation.

Open:

```text
index.html
```

Notes:
- No PHP server is required.
- No MySQL database is required.
- Data is stored in browser `localStorage`.
- Jikan/MyAnimeList image URLs are used for cover images.

